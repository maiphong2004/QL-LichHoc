Thư viện của React Native (từ react-native):

StyleSheet
View
ScrollView
Modal
TextInput
Button
Platform
TouchableOpacity
Alert
Pressable
FlatList
Thư viện của React (từ react):

React (được import ngầm định khi sử dụng JSX, nhưng cũng có thể được import rõ ràng)
useState (hook)
useEffect (hook)
useMemo (hook)
useLayoutEffect (hook)
Thư viện của Expo:

expo-router (được sử dụng cho hệ thống định tuyến, đặc biệt là Tabs và Link)
@expo/vector-icons (cung cấp các bộ icon như Ionicons, MaterialIcons)
Thư viện bên thứ ba (thường cần cài đặt qua npm hoặc yarn):

@react-native-community/datetimepicker (dùng cho việc chọn ngày và giờ)
@react-native-picker/picker (dùng cho component Picker - menu xổ xuống)
moment (thư viện xử lý ngày giờ rất phổ biến, cần thêm moment/locale/vi để hỗ trợ tiếng Việt)
@react-navigation/native (cho useNavigation hook, có thể dùng thay thế expo-router cho Link nếu không dùng Expo Router)
@react-navigation/bottom-tabs (dùng để tạo Bottom Tab Navigator trong BottomTabNavigator.tsx)
Các module/component tự định nghĩa trong dự án (thường nằm trong thư mục @/):

@/components/ThemedText
@/components/ThemedView
@/components/HapticTab
@/components/ui/TabBarBackground
@/constants/Colors
@/hooks/useColorScheme
@/context/AppContext (đặc biệt là các kiểu dữ liệu HomeworkItem, ScheduleEvent)
@/app/(tabs)/components/AddHomeworkModal (là một component bạn tự tạo)
@/app/(tabs)/components/AddScheduleModal (là một component bạn tự tạo)
@/app/(tabs)/ScheduleScreen (là một component màn hình bạn tự tạo)
@/app/(tabs)/HomeworkScreen (là một component màn hình bạn tự tạo)